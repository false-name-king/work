# 生产数据记录系统 - 开发文档 (v2 重构版)

## 1. 项目概述
本项目是一个面向移动端（最小适配 370px）的 Web 应用程序，主要用于生产组长/机长记录和管理团队成员的每日生产数据、出勤状态及加班时间，并支持一键生成规范的文本报表用于汇报。

## 2. 核心功能统计

### 2.1 全局配置
*   **日期记录**: 记录当前数据所属日期（默认当天，如 "8 月 28 日"），支持手动修改。
*   **产量备注**: 记录当天的特殊情况（如大批量、奖金情况等），默认为 "无"。

### 2.2 组员数据管理 (核心列表)
列表中的每个组员拥有独立的编辑卡片，包含以下字段：
*   **基础信息**: 姓名、角色（机长/组员）、机号（0-99）。
*   **出勤状态**: 单选（出勤 / 请假 / 公休 / 产假）。
    *   *逻辑分支*: 若选择非“出勤”状态，则隐藏下方所有生产和工时配置，仅展示状态占位图。
*   **工作类型 (仅“出勤”时可见)**: 多选（人工、引导、闸机、货检、商务、未进岗）。
*   **计件产量 (仅“出勤”时可见)**: 选择批数（0-199）和件数（0-199）。
*   **加班工时 (仅“出勤”时可见)**: 原生时间选择器（HH:mm - HH:mm）。

### 2.3 自动汇总与报表输出
*   **实时汇总**: 页面底部实时计算所有出勤人员的 `总批数` 和 `总件数`。
*   **报表文本生成**:
    *   **复制报表**: 格式化输出人员机器、角色、姓名、产量、工作状态及整体汇总和备注。
    *   **复制时间表**: 格式化输出所有人员的上下班时间。

### 2.4 页面导航
*   底部或顶部的 Tab 切换（数据视图 / 日期历史视图）。

---

## 3. 性能优化驱动的新架构设计

之前的卡顿根本原因在于：**复杂对象（Dayjs）被存入全局状态**，且**状态颗粒度拆分不合理**导致 React 的 Diff 算法在处理列表时负荷过重。

### 3.1 状态管理优化 (Zustand)
绝对禁止在 Store 中存储不可序列化的对象（如 Dayjs 实例）。
新状态结构将完全扁平化，时间仅使用 `"HH:mm"` 字符串存储：
```typescript
interface Person {
  id: number;
  name: string;
  role: string;
  machine: number | null;
  attendance: "出勤" | "请假" | "公休" | "产假";
  workStatus: string[];
  batches: number;
  pieces: number;
  startTime: string; // "19:00"
  endTime: string;   // "20:30"
}
```

### 3.2 渲染性能优化
*   **虚拟化/隔离**: 虽然列表不长，但将使用严格的 `React.memo` 配合基础数据类型，确保只有被修改的那个输入框及其父卡片重新渲染。
*   **防抖输入**: 文本框输入将脱离全局状态的实时双向绑定，采用 Local State + `onBlur` 或防抖再同步到 Zustand，切断输入时的全局渲染链。

### 3.3 UI 与样式栈
*   **框架**: Vite + React 19 + TypeScript.
*   **UI 库**: shadcn/ui (通过 `pnpm dlx` 安装确保网络畅通)。
*   **样式**: Tailwind CSS (移动端优先，使用细粒度 utility class，杜绝冗余的 CSS-in-JS 运行时开销)。
